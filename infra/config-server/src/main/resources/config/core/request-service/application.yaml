server:
  port: 0

spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: false
  sql:
    init:
      mode: always
  datasource:
    driver-class-name: org.postgresql.Driver
    # url: jdbc:postgresql://postgres-ewm-requests-db:5432/requests-db # Вариант 1: Использовать имя контейнера и внутренний порт (если запускаем в контейнере)
    url: jdbc:postgresql://localhost:5433/requests-db # Вариант 2: Использовать localhost и внешний порт (если запускаем на хосте)
    username: dbuser
    password: 123456

# actuator
management:
  endpoints:
    web:
      exposure:
        include: "health,info,mappings"
  endpoint:
    mappings:
      enabled: true

logging:
  level:
    ru.practicum.requests: DEBUG
